/**
 * @deprecated Use `@/lib/application/market-alert` directly for new code.
 * Re-export barrel maintained for backward compatibility.
 */
export {
  requestNotificationPermission,
  getNotificationStatus,
  checkMarketAlerts,
  type MarketAlertResult,
} from "./application/market-alert";
